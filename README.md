# Alternative Data Alpha Generator (skeleton)

This repo tests whether **alternative data** (e.g., Reddit/Twitter sentiment) helps predict stock returns.

## Quick start

```bash
python -m venv .venv
source .venv/bin/activate   # on Windows: .venv\Scripts\activate
pip install -r requirements.txt
